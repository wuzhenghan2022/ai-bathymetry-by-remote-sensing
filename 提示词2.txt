用joblib加载weights/ChatGPT4o.pkl模型（LinearRegression），然后加载data/image.csv文件中真实的的遥感反射率数据，它的大小为391*376行，多列，第0列为经度信息，第1列为纬度信息，第3-最后一列对应不同的光谱反射率数据。现在需要你用这个模型反演港口水深。将你的预测结果转化为391，376大小，添加颜色映射为Spectral，并输出最终的水深图。横坐标刻度显示标准化（度，分，秒）经度，纵坐标刻度显示标准化（度，分，秒）纬度数据。把生成的数据输出为Excel（三列：经度，纬度，水深），还有python代码。
