data.csv文件第一列是目标值y，后面的列为x1，x2...x4代表水深特征。请你分析文件中的数据，选择知识库中最合适的模型进行拟合工作。你需要首先将csv文件随机抽取n(n为一个变量设为1000)条数据分为训练数据集，将整个数据集预测数据集,训练模型。

然后加载data/image.csv文件中真实的的遥感反射率数据，它的大小为391*376行，多列，第0列为mask信息，第1列为经度信息，第2列为纬度信息，第3-最后一列对应不同的光谱反射率数据。现在需要你用这个模型反演港口水深。将你的预测结果转化为391，376大小，添加颜色映射为Spectral，刻度范围为0-15m。并输出最终的水深图。横坐标刻度显示标准化（度，分，秒）经度，纵坐标刻度显示标准化（度，分，秒）纬度数据。把生成的数据输出为Excel（三列：经度，纬度，水深），还有python代码。将反演结果中，对应mask为黑色的部分（阈值<128）在反演图中设置为白色。